{
  "id": "trans-paper",
  "name": "论文翻译服务",
  "category": "工具类",
  "type": "service",
  "status": "draft",
  "summary": "把 pdf2zh-next 包成可启动的论文翻译服务。",
  "description": "这是一个工具服务项目。现在已经接入 dashboard，可以做配置检查、启动和停止。默认推荐 Google 翻译引擎，开箱就能用；如果你想用 QwenMT，再去 setup.bat 里填 API Key。",
  "path": "E:/Project/codex/TransPaper",
  "entry": "http://127.0.0.1:7862/",
  "health": "http://127.0.0.1:7862/",
  "integration": "project-config",
  "control": {
    "enabled": true,
    "type": "service",
    "actions": ["test", "run", "stop"],
    "testCommand": {
      "command": "powershell",
      "args": ["-NoProfile", "-NonInteractive", "-WindowStyle", "Hidden", "-ExecutionPolicy", "Bypass", "-File", "scripts/test.ps1"]
    },
    "runCommand": {
      "command": "powershell",
      "args": ["-NoProfile", "-NonInteractive", "-WindowStyle", "Hidden", "-ExecutionPolicy", "Bypass", "-File", "scripts/run.ps1"]
    },
    "stopCommand": {
      "command": "powershell",
      "args": ["-NoProfile", "-NonInteractive", "-WindowStyle", "Hidden", "-ExecutionPolicy", "Bypass", "-File", "scripts/stop.ps1"]
    },
    "processMatch": {
      "name": "python.exe",
      "commandLinePattern": "*pdf2zh_next.main*--server-port 7862*"
    },
    "startupProbe": {
      "initialDelayMs": 5000,
      "attempts": 40,
      "intervalMs": 3000
    }
  },
  "paths": {
    "config": "config",
    "logs": "logs"
  },
  "timeline": [
    {
      "time": "今天",
      "title": "接入真实控制层",
      "note": "dashboard 现在可以检查配置、启动和停止这个项目。"
    }
  ]
}
